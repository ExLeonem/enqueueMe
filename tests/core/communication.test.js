const { getConfig, writeConfig } = require('../../core/testUtils');
const MessageMock = require('../../core/messageMock');


test("No channels configured", () => {

});


test("No category configured", () => {

});


test("No channels/category configured", () => {

});


test("Array of categories configured", () => {

});


test("Array in channels configured", () => {

});